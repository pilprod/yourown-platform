# contracts

Status: planned unless explicitly stated otherwise.

Planned public contracts: environment binding, workload, immutable release, secret reference and administrative MCP capability. Logical references are public; bindings, CIDRs, origins and credentials are private. JSON Schema plus negative/compatibility tests are required before a contract is declared implemented.
