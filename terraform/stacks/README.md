# stacks

Status: planned unless explicitly stated otherwise.

Implement provider-specific foundation, network, runtime, data and delivery compositions. Cloudflare edge/admin ownership is separate. Validate actual Stacks syntax and private input injection before claiming a configuration is usable.

Azure will have foundation, network, runtime-container-apps, runtime-aks, data and delivery compositions. Those are lifecycle boundaries, not an instruction to create every component for every product. Azure private Blob snapshot loading and phase-aware AzureRM OIDC wiring remain acceptance work.
