# aws

Status: planned unless explicitly stated otherwise.

First slice: account binding, identity and private configuration. ECR/ECS follows after bootstrap; EKS/RDS and expensive network defaults are opt-in.
